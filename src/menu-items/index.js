import dashboard from './dashboard';
import options from './options';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, options]
};

export default menuItems;
